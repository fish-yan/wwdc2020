import 'package:flutter/material.dart';

class OtherPage extends StatelessWidget {
  final bool isPic;
  OtherPage({this.isPic = true}) : super();
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Container(),
      ),
    );
  }
}